<h1>Listing stats <span style="font-size:14px;">Past 30 minutes</span></h1>

<h3>Average chais/day: <%= @avgdaychains %> <span style="font-size:14px;">(<i class="<% if @changedaychains > 0 %>fa fa-arrow-up green<% elsif @changedaychains < 0 %>fa fa-arrow-down red<% else %>fa fa-minus<% end %>"></i> <%= @changedaychains %>)</span></h3>
<h5>Max: <%= @stats.find(:all).map(&:chains).max %> Min: <%= @stats.find(:all).map(&:chains).min %></h5>


<table>
  <thead>
    <tr>
      <th>Server</th>
      <th>Time</th>
      <th>Primes/s</th>
      <th>Tests/s</th>
      <th>5-chains/h</th>
      <th>Chains/d</th>
    </tr>
  </thead>

  <tbody>

    <% @stats.each do |stat| %>
      <tr>
        <td><%= stat.server_id %></td>
        <td><%= stat.date %></td>
        <td><%= stat.primes %></td>
        <td><%= stat.tests %></td>
        <td><%= stat.fivechains %></td>
        <td><%= stat.chains %></td>

      </tr>
    <% end %>
  </tbody>
</table>

<br>

